# このワークフローは無効化しています。
# GitHub Pages は「Deploy from a branch (gh-pages)」で運用しています。
# 設定場所: Settings → Pages → Source: Deploy from a branch → gh-pages / (root)

name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:   # 手動実行のみ（自動トリガー無効）

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ワークフローは無効化されています。gh-pagesブランチから直接公開しています。"
